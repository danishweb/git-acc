# git-acc (working title)

CLI to switch between multiple GitHub accounts locally (SSH keys + per-repo identity).

## Status
WIP.

## Roadmap
- [ ] MVP: add/list/use accounts
- [ ] Per-repo config and origin rewrite
- [ ] CI + tests
